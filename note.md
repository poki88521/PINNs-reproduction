## 简介
pinn复现过程中的日记，内容包括但不限于知识笔记、收获总结、问题提出、碎碎念等，较为杂乱，按日期分隔

## 内容
### 2026-6-20
- 只看原论文把整个代码重新写出来有点难的离谱了。。。假期真的能把10篇复现完吗。。。
- 感觉要把之前没完全看完的附件看完了（偷懒的惩罚来了）
- 还是有好消息的：之前完全没考虑到为什么模型不谈epoch只谈iteration和batch，这次必须要考虑这一点。算是彻底搞清楚pinns模型的数据都是怎么来和怎么用的了
- 我决定继续这样重写代码，虽然可能要花很久很久
- ds怎么ai味突然这么冲了。。。
- 对于之前考虑过的通过dropout来防止过拟合，实际情况是pinns模型本身就不容易发生过拟合（因为使用的数据集不固定）

### 2026-6-28
- pytorch会不了一点。。。用手写识别找回手感
- 权重归一化大多数都用的dim=0，且新版开始用的是torch.nn.utils.parameterizations.weight_norm()
- 之前跑的复现论文居然少一个隐藏层？？？
- 自己写的代码将保持10个隐藏层+2输入输出层

### 2026-6-29
- 别人复现的代码里`t_data = T_star[:, idx_t][idx_x, :].flatten()[:, None]`看了有三个小时
- 列表索引里面好像是可以塞数组的，而且reshape是有其他写法的。。。。
- 计算loss_c和loss_e需要的数据集是需要分别抽的
- ds半夜1：40叽里咕噜说胡话了开始

### 2026-6-30
- 数据集里面的名字居然是大小写不一样的
- ds依旧抽风
- 我怀疑ds的上下文到了，开始卡死了

### 2026-7-1
- 目前还没搞明白如何用Pycharm输入命令行参数，这个还得学学（目前都是通过直接改代码来完成的）
- 家里的台式机的github访问一直有问题，有时间最好修好这个问题，但git已经安装了，争取以后可以只通过github进行设备交互
- 关于之前的网络对模型结果的影响，目前看来影响不大（我估计压强的预测不是网络结构导致的，因为网络结构大概率会同时影响所有的输出）
- 早停法我也还没加进来，根据我观察error曲线画出来的图，我认为早停法还是很有必要的，但是早停法的停止判定标准还有待进一步研究
- 最优模型的存档并没有考虑到error的事情，只是保存了总损失最低的模型，后续可能考虑把这一点也加入评估范围
- ds提到的注意loss_c和loss_e的差距是否超过一个数量级，根据我自己观察数据没有出现这种问题所以没有多操作，但这应该是一个需要考虑的事情，后续可能考虑加入loss的权重$\lambda$
- 实际上尽管我用了ds提供的中心化后的error计算方法，压强的error依然是不断升高的（而且波动不小）（当然还是比寒假时候的那个模型好多了）但我看最佳模型的压强色彩云图的问题不大（和原论文比没什么大问题）所以我暂时忽略这一error异常的问题
- 目前大概是对常用的读取数据、设计模型、进行训练、评估损失的逻辑流程和代码大概熟悉了，比我几天前预想的进度要快，预计从下一个论文开始就不完全自己手写复现代码了，但这种手写代码还是对读源代码有帮助的
- 考虑到以后的论文大概率不能在我已经配置的环境中正常运行（HFM的原论文是Tensorflow的，既然我还没复现到更现代的论文，那么Tensorflow为框架的源代码还会出现很多），以后复现重写代码的机会可能还会比较多
- 这次是完全严格按照论文所提到的方式来规划模型设计、训练和评估的（甚至细化到batch_size是否和原论文相同），这和寒假时候做的事情完全不同，不过结果还是比寒假时候看起来稳定很多
- 更好的事情是我有机会用上家里的算力，可以不用单独给腾讯云花钱（而且效率还不错），不知道目前模型的体量在以后可能复现的模型中体量算不算大，如果不算大的话我可以继续放心的用家里的算力了（目前比较担心的就是3D的情况，这种情况的数据集、算力需求都会比2D情况大很多，最好的情况就是我可以继续把论文中的方法降维到2D中进行复现）（我还记得动脉瘤的数据集有大几个G的大小，3D模型在家用电脑上跑不起来的情况很可能是真的，那个时候恐怕真的需要用云算力了）
- 今天的日记是一个阶段性的总结，已经基本把目前还在做的事情、准备尝试做的事情都已经总结完了，所有学到的东西、已经完成的东西都写在了日志和项目总结板块里面。总体而言收获还是很大的（无论是代码理解层面还是PINNs基础的层面）但我目前还对PINNs的发展以及最前沿的内容没有任何概念（而且短期内可能也了解不到哪里因为准备按ds之前的计划复现），可能这要等再过几个月了
- 下一个复现的论文涉及到了精度提升，这也是我目前期待解决的问题，我期待这篇论文能帮我解决的问题不只是精度提升，我观察已久的压强不收敛/收敛慢/收敛效果差的问题也能一并解决（至少提供思路）（我不知道为什么上次加了压强的泊松方程约束的模型还是没法降低压强的error，可能与我之前没用上新的error计算标准有关，也可能与之前迭代次数不够有关，这个还有待进一步尝试）
- 说到寒假时做的模型，我也期望下一篇复现的论文能提供解决此模型问题的理论、方法（寒假时候的模型问题主要集中在压强的损失随着迭代越来越高甚至越来越不收敛，我暂且不知道是评估标准还是压力泊松方程约束的问题，那个模型的变量太多了而且没控制好）

### 2026-7-5
- 这一次复现还有一个小细节：原文用了很多希腊字母。那么命名变量是用功能还是希腊字母英语拼写
- 原论文采用的是Helmholtz方程，但HFM的例子使二维圆柱绕流（ns方程），据说后者的复现难度会大一点

### 2026-7-6
- 原论文有个matplotlib的语法错误所以一直报错跑不起来
- 与nn.Sequential相比，nn.ModuleList是一个不自带传播方向的集合，可以用此类实现更复杂的连接方式

### 2026-7-8
- HFM的代码中没有设置随机种子，以后应该进行设置
- HFM中也没有进行权重初始化，应该同时进行权重初始化和归一化
- 突然意识到最开始的计划是用deepxde进行hfm的复现，但也还是用torch复现完了

### 2026-7-9
- 进度条视觉效果还不错，学了

### 2026-7-27
- 上完课可以继续弄论文了。。。
- 一个暂未解决的问题是还不会用命令行给一个python传参数（如果学不会这个可能会考虑直接在文件中修改参数）
- 目前考虑的一个理想状态是不同的算例可以在一个完整的python项目中进行切换（可以先制作一个框架）~~顺便水github~~

### 2026-7-28
- 传参数和写.bat的操作基本会了
- 目前考虑按照ds建议把.yaml作为配置用的文件
- ds的项目骨架建议：
```text
pinn-framework/
├── configs/                    # YAML 配置（算例、模型、训练参数）
├── src/
│   ├── problems/               # 物理问题定义
│   │   ├── base.py             # 定义 PDE、BC、IC 的接口
│   │   ├── helmholtz.py        # 继承 base，实现 Helmholtz
│   │   └── burgers.py          # 后续扩展
│   ├── models/                 # 网络结构
│   │   ├── base_net.py         # 标准 FNN（对照组）
│   │   └── attention_net.py    # 你手里的 Net_attention（创新组）
│   ├── trainers/               # 训练控制器（关键！）
│   │   ├── base_trainer.py     # 定义训练、评估、存档接口
│   │   ├── dde_trainer.py      # 利用 dde 快速实现对照组训练
│   │   └── custom_trainer.py   # 原生 PyTorch 训练（用于创新算法）
│   ├── utils/                  # 工具（可视化、日志、存档）
│   └── main.py                 # 统一入口（解析 YAML + 调度）
└── runs/                       # 实验存档（模型、日志、图片）
```

### 2026-8-3
- ds居然在假设我用的优化器是L-BFGS。。。
- 忽然考虑了一下多算例的问题。在这次之间我几乎没有进行多算例的实践（准确来讲只接触了圆柱绕流2D和亥姆霍兹2D两个算例），但无论是对于PINN原论文、HFM还是I-PINN的论文，都使用了至少两个算例。所以目前来看多算例的集成项目是非常有必要的。

### 2026-8-9
- 今天来不及继续写代码了，就总结一下之前有关PINN算例的内容
- 基本分类：稳态问题和瞬态问题，正向问题和逆向问题、微分方程的数学性质（椭圆型、双曲型、抛物型）
- 稳态与瞬态指该问题是否与时间相关。稳态问题不与时间相关而瞬态问题相关。瞬态问题要在考虑边界条件的基础上考虑初始条件
- 正向与逆向问题指全局物理参数是否是学习出来的。两种问题可以被统一的结构解出来（因为网络只需满足总体符合数据残差和物理残差最小）
- 椭圆型、双曲型、抛物型都是偏微分方程（PDE）的数学分类。通常认为解决难度椭圆>抛物>双曲

### 2026-8-26
- 有点受不了python了。。。什么叫类型自己猜。。。什么叫“反射”满天飞。。。
- 另外这个项目开始规模有点大了，感觉有点难以处理。。。

### 2026-8-29
- 花了半个多月把这个项目写完，了解了很多对PINN相关内容的概览（或者说是一个整体固定的框架）。比如说损失函数基本都可以写成“方程残差+边界条件+初始条件”这样，比如说稳态问题和瞬态问题的区别，椭圆双曲抛物型方程的区别。也算是弥补了没仔细读PINN开山论文缺的功课吧。
- 为了写这个项目给电脑接了agent，买了deepseek的token，折腾了相当长一段时间的agent插件（说的就是卡的不能用的continue vscode版）也算是意外收获吧。
- 写这个项目感受到上一个项目（HFM）的视角其实有些局限（纯流体力学问题、正向问题），现在的视角开阔多了——了解到有不同方面不同难度的问题，所求解的东西也不一样。视角的开拓也得益于了解了deepXDE（dde）。dde作为一个适合于做pinn的包，已经尽可能尝试将所有pinn问题格式化为一个框架，使用dde可以更好的体会“概览”的感觉。
- 算例侧的问题基本解决，接下来比较缺失的知识是改进侧的。这个只能通过复现不同论文来学，但不知道有没有机会形成一个比较体系化的架构。目前可以体系化的是改进的位置，在整个流程（准备和处理数据——准备模型——训练前处理——训练——训练后处理——评测）中不同地方插入内容或修改其中的部分。
- 复现结果出来了。l2和损失都比std降的快，而且更低更稳定。至于AllenCahn的瞎拟合（？）问题，那是采样的事情。之后如果有包含自适应采样的问题会再注意这里的。
- 这篇论文引用的论文极其多，个人认为有一定的“综述”性质，可以代表相当一部分的论文，代表一种很主流的pinn优化方向（网络和损失函数），比较有典型性。
- 另外注意到PINN-bench项目避开了ns方程的有关内容。流体力学算例拓展将依赖算例拓展接口、存档等功能的实现，较为困难但确实是一个很有价值的内容。避开这个内容完全“不小心”的，以下是流体力学算例的复杂性：
    + 通常没有解析解，需要读数据集，这是一个额外代码开销
    + 物理约束条件通常比较复杂，导致其训练时间较长，时间开销增加（这个事情已经体现在了上一篇复现的论文中）
    + 由于其训练时间长，没有存档功能会使训练风险较大，这也是额外代码开销
    + 流体力学本身内容也比较多，算例众多，选择算例也是一个重要问题
- 考虑独立设计流体力学算例验证方式？
- HFM里面的边界条件是个啥？花型？
- 以下为deepseek总结的有关PINN流体力学的内容
```
**PINN流体力学算例对比与学科概述**
### 方腔流 vs 圆柱绕流：核心差异
| 维度 | 顶盖驱动方腔流 | 圆柱绕流 |
|------|---------------|----------|
| 几何边界 | 封闭方腔，顶角速度不连续（奇异性） | 外部绕流，含圆柱表面与远场 |
| 流动性质 | **稳态**，主涡+角涡结构 | **非稳态**，高Re时形成卡门涡街 |
| PINN挑战 | 边界奇异性处理 | 时空动态捕捉、涡脱频率预测 |
| 参考基准 | Ghia基准解（1982） | DFG基准 |
**互补性**：方腔流检验PINN处理"边界奇异性+封闭域"的基础能力，是"单元测试"；圆柱绕流检验"复杂几何+非定常涡旋"的综合能力，是"综合大考"。
### PINN流体力学应用分类
| 类别 | 说明 |
|------|------|
| 正问题 | 给定方程/边界，求解流场（稳态/非稳态） |
| 逆问题 | 从稀疏数据反推参数或重构流场 |
| 代理模型 | 快速替代传统求解器，用于设计优化 |
| 湍流建模 | 学习雷诺应力、亚格子模型、校准封闭系数 |
### PINN流体力学核心难点
1. **高Re刚性**：对流主导→损失函数刚性→优化困难
2. **频谱偏差**：偏向低频特征，难以捕捉高频小尺度涡
3. **损失不平衡**：PDE残差、BC、IC多项梯度量级差异大
4. **复杂几何**：边界采样与精确施加边界条件困难
5. **计算成本+泛化性**：单模型仅适用单工况，新工况需重训练
6. **多尺度/多物理场**：跨尺度涡与流-固-热耦合增加复杂度
```
- 值得注意的是我认为目前复现的两篇论文虽然非常有典型性，但实际上距离比较远。

### 2026-8-31
- 这个论文的复杂程度要比第二篇高，不仅是改进点的数量，而且损失函数里加了一个比较复杂的算子

### 2026-9-2
- 去学信号与系统了。这本身是其他项目的任务，但是会一定程度上帮助理解论文。
- “对一个项目如果有想法那就尽快把第一版制作出来（而不管其完整度和拓展功能）”

### 2026-9-5
- 不太看得懂jax的代码让ds帮我读了
- 这篇论文感觉和前一篇风格差别好大。。。
- 学习了余弦退火算法：单个周期内学习率越来越小，直到下个周期开始学习率突然增大，以此不断寻找平坦最优解
$$\eta_t = \eta_{\min} + \frac{1}{2}(\eta_{\max} - \eta_{\min}) \left(1 + \cos\left(\frac{t}{T} \pi\right)\right)$$
- 这篇论文将采样点“固定”了（总样本就是数据集，每次从里面随机抽）